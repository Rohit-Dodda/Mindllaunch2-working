const Loader = () => {
  return (
   <span className="loader ml-20" 
   style={{
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "column",
  }}>
   
   </span>
  )
}

export default Loader